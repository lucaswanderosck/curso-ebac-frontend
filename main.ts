const multiply = (numberOne: number, numberTwo: number): number => {
  return numberOne * numberTwo;
};

console.log(multiply(45, 14));

const greeting = (userName: string): string => {
  return `Olá ${userName}`;
};

console.log(greeting("lucas"));
